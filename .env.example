# Database
# API 서버 VM: 로컬 docker-compose의 db 서비스를 가리키면 됨 (host=localhost 또는 db)
# 배치 서버 VM: 다른 VM에서 도는 것이므로 반드시 DB에 접근 가능한 실제 호스트/IP로 바꿀 것 (localhost 아님)
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
DATABASE_URL=

# External APIs
ADZUNA_APP_ID=
ADZUNA_APP_KEY=

# BigQuery
GOOGLE_APPLICATION_CREDENTIALS=
BIGQUERY_PROJECT_ID=
