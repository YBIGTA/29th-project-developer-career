# 이 파일을 frontend/.env.local로 복사해서 쓴다.
#
# 주의: Next.js는 프로젝트 루트(= frontend/)의 .env.local만 읽는다.
# 저장소 루트의 .env.local은 무시되므로 반드시 이 디렉터리 안에 둬야 한다.
#
# 비워 두면 lib/mockData.json / mockPostings.json으로 동작한다 (요청을 아예 보내지 않음).
NEXT_PUBLIC_API_URL=
